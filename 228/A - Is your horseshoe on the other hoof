#include <iostream>
#include <set>
 
using namespace std;
 
int main() {
    int s1, s2, s3, s4;
    cin >> s1 >> s2 >> s3 >> s4;
    set<int> horseshoe_colors;
    horseshoe_colors.insert(s1);
    horseshoe_colors.insert(s2);
    horseshoe_colors.insert(s3);
    horseshoe_colors.insert(s4);
    int horseshoes_to_buy = 4 - horseshoe_colors.size();
    cout << horseshoes_to_buy << endl;
 
    return 0;
}